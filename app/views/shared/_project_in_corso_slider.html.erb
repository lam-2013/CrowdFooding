<div id="myCarouseltab<%= @projectcat%>" class="carousel slide" data-interval="4000" data-pause="hover">
  <div class="carousel-inner" >
    <% if @projects_in_corso.any? %>
        <%  @projects_in_corso.each do |project|  %>


            <% if @projects_in_corso.first == project %>
                <div class="active item <%= project.id %>">
            <% else %>
                <div class="item <%= project.id %>">
            <% end %>

            <div class="carousel-cation">
              <ol class="projects">

                <div class="row-fluid" id="padre" >

                  <div class="row-fluid" id="info" >
                    <div class="span6">
                      <h1><%=link_to project.titolo ,project%> </h1>
                    </div>
                    <div class="span6">
                      <p> Città:<%= project.luogo %>       </p>
                    </div>
                  </div>

                  <div class="row-fluid" id="desc">
                    <div class="span6 offset1">
                      <%=link_to image_tag(project.img_copertina), project%>
                    </div>
                    <div class="span4">
                      <p>Di cosa si tratta?  </p>
                      <p><%= project.descrizione %>   </p>
                    </div>
                  </div>

                  <div class="row-fluid" id="barre" >
                    <div class="span8 offset2">
                      <p style="text-align: left">budget attuale: <%= project.budget_attuale %><p>
                      <div class="progress progress-success progress-striped active">
                        <%if (project.budget_attuale * 100 / project.goal > 100) %>
                            <div class="bar" style="width: 100%;"></div>
                        <% else %>
                            <div class="bar" style="width: <%= project.budget_attuale * 100 / project.goal %>%;"></div>
                        <% end %>
                      </div>
                      <p style="text-align: right">goal <%= project.goal %><p>
                      tempo rimanente:
                      <% giorni_rimanenti = ((project.data_fine- Time.zone.now)/1.day).to_i %>
                      <% ore_rimanenti = ((project.data_fine- Time.zone.now)/1.hours).to_i %>
                      <% minuti_rimanenti = ((project.data_fine- Time.zone.now)/1.minutes).to_i %>
                      <% secondi_rimanenti = ((project.data_fine- Time.zone.now)/1.seconds).to_i %>
                      <% if giorni_rimanenti <= 1 %>
                          <% if ore_rimanenti <= 1 %>
                              <% if minuti_rimanenti <= 1 %>
                                   <%= secondi_rimanenti %>   s
                              <%else%>
                                   <%= minuti_rimanenti /24 %> :
                                   <%= secondi_rimanenti /60 %>  (m:s)
                              <%end%>
                          <% else %>
                                    <%= ore_rimanenti %> :
                                    <%= minuti_rimanenti / 24 %> (h:m)
                          <% end %>
                      <% else %>
                         <%=  giorni_rimanenti %> giorni
                      <% end %>
                      <div class="progress progress-danger progress-striped active">
                        <div class="bar" style="width: <%= 100-ore_rimanenti * 100 / 672 %>%;"></div>
                      </div>

                    </div>
                  </div>
                </div>
              </ol>
            </div>
            </div>

        <% end %>
    <% end %>
    </div>
    <a class="carousel-control left" href="#myCarouseltab<%=@projectcat%>" data-slide="prev">&lsaquo;</a>
    <a class="carousel-control right" href="#myCarouseltab<%=@projectcat%>" data-slide="next">&rsaquo;</a>

  </div>
</div>